"""UI Capabilities: LLM-discovered, deterministically replayed UI automation.

The model discovers; the artifact is the capability; deterministic replay is
production execution.
"""

__version__ = "0.1.0"
